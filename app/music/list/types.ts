export type Music = {
  music_id: string;
  music_name: string;
  cover_url: string;
  length: number;
  // rank: number;
  // thumbnail: string;
  // title: string;
  // author: string;
  genre: string;
  // time: string;
};
